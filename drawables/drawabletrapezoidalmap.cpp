#include "drawabletrapezoidalmap.h"

/*DrawableTrapezoidalMap::DrawableTrapezoidalMap(const Trapezoid& boundingBox) {
    TrapezoidalMap(boundingBox);
    colors = std::vector<cg3::Color()>();
    colors.push_back(randomColor());
}

void DrawableTrapezoidalMap::draw() const {
    for(size_t i=0; i<trapezoids.size(); i++) {
        Trapezoid t = trapezoids[i];
        cg3::opengl::drawQuad2(
                    t.getTop().p1(), t.getTop().p2(),
                    t.getBot().p2(), t.getBot().p1(),
                    cg3::Color(255, 0, 0),
                    1, true);
    }
}

const cg3::Color DrawableTrapezoidalMap::randomColor() const {
    return cg3::Color(255, 0, 0);
}
*/
